# 🧪 Тестирование производительности

## Быстрая проверка

### 1. Автоматическое определение

Просто откройте сайт и посмотрите в консоль (F12):

```
🔍 Device Detection: {
  cores: 4,
  memory: '8GB',
  isMac: true,
  isOldMac: false,
  isSafari: true,
  screenWidth: 1440,
  isSmallScreen: true,
  isLowEndDevice: true  ← Режим оптимизации включен!
}
```

Если `isLowEndDevice: true` - оптимизации активны! 🚀

### 2. Ручное включение режима оптимизации

В консоли браузера:

```javascript
// Включить режим слабого устройства
window.performanceOptimizer.forceLowEndMode()
location.reload() // Перезагрузить страницу

// Выключить режим
window.performanceOptimizer.forceHighEndMode()
location.reload()
```

### 3. Проверка FPS

После загрузки страницы система автоматически замерит FPS и выведет в консоль:

```
📊 Average FPS: 55
```

Если FPS < 30 - система автоматически включит оптимизации.

## Детальное тестирование

### Chrome DevTools Performance

1. Откройте DevTools (F12)
2. Вкладка **Performance**
3. Включите CPU throttling: **6x slowdown** (эмуляция слабого железа)
4. Нажмите **Record** и прокрутите сайт
5. Остановите запись и проверьте:
   - **FPS** должен быть > 30
   - **CPU usage** должен быть < 80%
   - **Main thread** не должен быть перегружен (зеленые зоны)

### Network throttling

1. DevTools → **Network** tab
2. Выберите **Fast 3G** или **Slow 3G**
3. Перезагрузите страницу (Ctrl+R)
4. Проверьте время загрузки:
   - **Fast 3G**: < 3 секунд
   - **Slow 3G**: < 8 секунд

### Lighthouse

1. DevTools → **Lighthouse** tab
2. Выберите:
   - ✅ Performance
   - ✅ Desktop или Mobile
3. Нажмите **Analyze page load**
4. Целевые показатели:
   - **Performance**: > 85/100
   - **First Contentful Paint**: < 1.5s
   - **Time to Interactive**: < 3s
   - **Largest Contentful Paint**: < 2.5s
   - **Cumulative Layout Shift**: < 0.1

## Что должно отключиться в режиме оптимизации

### ✅ Полностью отключено:
- Все blur эффекты
- Все backdrop-filter
- Все частицы (particles)
- 3D эффекты карточек (flip animation)
- Градиентные фоны
- Все shadows (box-shadow, text-shadow)
- Filter эффекты
- **Timeline автопрокрутка** (кнопка ▶ по умолчанию)

### ⚡ Упрощено:
- Анимации отключены или сильно замедлены
- Transitions убраны
- Backgrounds заменены на solid цвета

## Визуальная проверка

### До оптимизации:
- 🌊 Видны анимированные lava blobs в Hero
- ✨ Много анимированных частиц
- 💫 3D эффект переворота карточек в Services
- 🎬 Timeline автоматически прокручивается
- 🌈 Красивые градиенты и shadows

### После оптимизации:
- ⬛ Простые черные фоны
- 📦 Плоские карточки без эффектов
- ⏸️ Timeline НЕ прокручивается автоматически
- 🎯 Минималистичный дизайн
- ⚡ **НО РАБОТАЕТ БЫСТРО!**

## Критерии успеха

### На старом MacBook (2015-2018):
- ✅ FPS > 30
- ✅ Страница не тормозит при скролле
- ✅ Нет "подвисаний" при переключении секций
- ✅ Анимации плавные (где есть)
- ✅ Время загрузки < 2 секунд

### На очень слабом железе (2010-2014):
- ✅ FPS > 20
- ✅ Сайт открывается и работает
- ✅ Весь контент доступен
- ✅ Навигация работает

## Проблемы и решения

### "Сайт всё равно тормозит"

1. Откройте консоль и проверьте:
```javascript
window.performanceOptimizer.isLowEnd() // Должно быть true
```

2. Если `false`, включите принудительно:
```javascript
window.performanceOptimizer.forceLowEndMode()
location.reload()
```

3. Проверьте класс в HTML:
```javascript
document.documentElement.classList.contains('low-end-device') // true
```

### "Анимации всё еще работают"

Проверьте в DevTools → Elements:
- У `<html>` должен быть класс `low-end-device`
- В `<head>` должен быть `<style id="performance-optimizations">`

### "FPS не измеряется"

Подождите 10 секунд после загрузки страницы - система автоматически замерит FPS.

## Рекомендации по дальнейшей оптимизации

Если производительность всё еще неудовлетворительна:

1. **Уменьшите разрешение изображений** - используйте srcset для разных размеров
2. **Добавьте code splitting** - React.lazy для тяжелых компонентов
3. **Используйте виртуализацию** - для длинных списков (react-window)
4. **Оптимизируйте шрифты** - используйте font-display: swap
5. **Включите service worker** - для кеширования ресурсов

## Monitoring в production

Добавьте в `main.tsx`:

```typescript
// Отправка метрик в аналитику
window.addEventListener('load', () => {
  performanceOptimizer.measureFPS((fps) => {
    // Отправить в Google Analytics / Яндекс.Метрику
    if (typeof gtag !== 'undefined') {
      gtag('event', 'performance', {
        fps: fps,
        device_type: performanceOptimizer.isLowEnd() ? 'low-end' : 'high-end'
      })
    }
  })
})
```

---

**Важно**: Всегда тестируйте на реальных устройствах, эмуляция не даёт 100% точной картины!

