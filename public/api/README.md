# 📧 API форм обратной связи

## ⚠️ Этот проект использует Node.js бэкенд

Отправка писем настроена через **Express.js + Nodemailer** на порту **3070**.

PHP файлы больше **НЕ ИСПОЛЬЗУЮТСЯ**.

---

## 🚀 Настройка и запуск бэкенда

Смотрите подробную инструкцию в:
- **`BACKEND_SETUP.md`** - полная документация
- **`server/index.js`** - код сервера
- **`.env.example`** - пример конфигурации

### Быстрый старт:

1. **Установите зависимости** (если еще не сделано):
```bash
npm install
```

2. **Создайте `.env` файл** в корне проекта:
```env
# SMTP настройки (Яндекс или другой провайдер)
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=your-email@yandex.ru
SMTP_PASSWORD=your-app-password  # Пароль приложения!
RECIPIENT_EMAIL=EcoKozhevnikov@yandex.ru

# Порт бэкенда
PORT=3070
```

3. **Запустите бэкенд**:
```bash
npm run server
```

4. **Запустите фронтенд** (в другом терминале):
```bash
npm run dev
```

Или запустите оба одновременно:
```bash
npm run dev:full
```

---

## 🔒 Безопасность

Включено:
- ✅ **Rate Limiting** - 3 запроса за 15 минут с одного IP
- ✅ **Honeypot** - скрытое поле для защиты от ботов
- ✅ **Frontend Rate Limiting** - 30 секунд между отправками
- ✅ **CORS** - только с разрешенных доменов
- ✅ **Валидация данных** - проверка email, имени, сообщения

---

## 📡 API Endpoint

**POST** `http://localhost:3070/api/contact`

### Формат запроса:
```json
{
  "name": "Иван Иванов",
  "email": "ivan@example.com",
  "company": "ООО Компания",
  "phone": "+7 999 123-45-67",
  "message": "Текст сообщения",
  "honeypot": ""  // Должно быть пустым!
}
```

### Формат ответа (успех):
```json
{
  "success": true,
  "message": "Сообщение успешно отправлено!"
}
```

### Формат ответа (ошибка):
```json
{
  "success": false,
  "message": "Описание ошибки"
}
```

---

## 🧪 Тестирование

### Локально (без SMTP):
Если в `.env` не указаны SMTP настройки, бэкенд автоматически использует **Ethereal Email** для тестирования.

Ссылка на просмотр письма выведется в консоли:
```
📧 Тестовый режим - письмо отправлено!
📎 Просмотр: https://ethereal.email/message/xxxxx
```

### С реальным SMTP:
Настройте `.env` с Яндекс или другим SMTP провайдером.

---

## 📞 Поддержка

При проблемах смотрите:
- **BACKEND_SETUP.md** - детальная документация
- **server/index.js** - код сервера
- Логи в консоли при запуске `npm run server`

---

**Контакты:**
- 📧 info@instrument-fit.ru
- 📞 8 (800) 511-12-33

**Версия:** Node.js бэкенд (Express + Nodemailer)
